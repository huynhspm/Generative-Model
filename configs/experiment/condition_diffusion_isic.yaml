# @package _global_

# to execute this experiment run:
# python train.py experiment=segmenation_diffusion_isic

defaults:
  - override /data: isic.yaml
  - override /model: diffusion/condition_diffusion_module.yaml
  - override /callbacks: diffusion.yaml
  - override /trainer: ddp.yaml
  - override /logger: wandb.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

seed: 12345

task_name: "train_diffusion"

trainer:
  max_epochs: 2000
  devices: 1
  check_val_every_n_epoch: 5
  gradient_clip_val: 1.0

data:
  batch_size: 8
  num_workers: 20

model:
  net:
    denoise_net:
      img_channels: 3
      n_layer_blocks: 1
      channels: 64
      channel_multipliers: [1, 2, 4, 4]
    cond_net:
      _target_: src.models.components.embeds.MaskEmbedding
      in_channels: 1
      d_cond: 256

logger:
  wandb:
    name: "${now:%Y-%m-%d}_${now:%H-%M-%S}"
    project: "diffusion"
    tags: ${tags}
    group: ${data.dataset_name}

callbacks:
  gen_sample:
    grid_shape: [5, 5]
  metrics:
    IS: null
